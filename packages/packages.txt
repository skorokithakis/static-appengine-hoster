Place zips of your packages here.

For example, to make "import flask" work,
the "flask" package should be available as "flask.zip".

You can install them using pip by running:

pip install -t packages/ Flask
